#include <stdio.h>
struct Car{
        char *brand;
        char model[30];
        int power;

    };
int main(){

    struct Car car;
    car.brand = (char*)malloc(30*sizeof(char));==
    return 0;
}
