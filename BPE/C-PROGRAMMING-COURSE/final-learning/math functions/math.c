#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9); // square root
    double B = pow(2,4);// power
    int C = round (3.14);//rounds
    int D = ceil (3.14);//rounds up
    int E= floor(3.99);//rounds down
    double F = fabs (-152);//absolute value
    double G = log(3);//natural logarithm
    double H = sin(45);
    double I= cos(45);
    double J = tan(45);

    printf("\n%d",C);

    return 0;
}
